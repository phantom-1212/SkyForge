services:
  - type: web
    name: skyforge-server
    runtime: docker
    dockerfilePath: ./server/Dockerfile
    dockerContext: ./server
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        sync: false   # Set this manually in Render dashboard to your Vercel URL
    # IMPORTANT: You must mount the Docker socket in Render dashboard:
    # Add a Disk or use a privileged runner — see deployment guide.
